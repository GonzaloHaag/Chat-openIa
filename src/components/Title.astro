---
interface Props {
    title:string;
    className?:string; //para recibir una clase personalizada especifica del elemento
}

const { title,className } = Astro.props
---

<h2 class={`text-2xl sm:text-4xl text-white ${className} leading-snug`}>{ title }</h2>